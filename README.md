# Cova programming language

Usage:

	cova <command> [arguments]

Commands:

	new
        <template>
	run
        -c | --configuration <Debug|Release>
        
	test
	build
	clean
	serve
	restore
	version